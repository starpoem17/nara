# Prefill/decode probes and current batch fallback

Status: complete. Current mixed-five entrypoint: `uv run --locked python scripts/run_batch_fallback.py --output-dir NEW_OUTPUT_DIR`; default dev200, ON1024 only six-feature group, other4groups OFF, H6v2/v3 rules. User authorized stopping before layer/kernel investigation and falling back to full-batch waiting. Do not resume continuous refill or kernel debugging without new direction. Prior continuous scripts remain historical experiment entrypoints, not current policy.

Artifacts: `analysis/prefill_decode_probe/report.md`, summary.json, probes.json (selected tokens/top10 logprobs), plan.json/targets.json, source/, validation.json. Diagnostic scripts `probe_prefill_decode.py`, `summarize_prefill_decode.py`.

Test1 repeated same24 barrier vs continuous:24/24 raw output differences,6/144 label flips,63.952s vs60.612s. This time initial8 also differ, unlike previous diagnosis; API submission timing/internal prefill batching confounds exclusive attribution to completion waiting. Not a new full200 quality estimate.

Test2: first4 divergent cases by input order, no label selection. Common output prefix appended to original prompt to fix exact token history. Probe generation intentionally unconstrained/no thinking-budget masks; production mode unchanged. Prefill solo vs8, max_tokens1:3/4 different selected tokens, exact repeated twice. Decode start solo, inject7backgrounds only afterfirst token: first token/top10 scores identical acrossconditions; shared-history logprobs diverge from4th output token in4/4, selected tokens diverge at8th in3/4. Other case stays same16tokens despite score changes. All16 same-condition repetition pairs identical token IDs and recorded logprobs. These are phase sensitivity probes, not preserved historical KV tensor replay or specific-layer/FP4/kernel attribution. Stop at user-requested boundary.

Fallback `nara/batch_barrier.py`: BatchCompletionModel buffers early completions and releases whole batch in submission order; BatchedPredictor reuses existing groups/modes/cache/RAG/retry/context checks. No edits to frozen ContinuousPredictor or core prompt/model adapter. Max8 request waves; completion barrier also postpones retry/search followups until current wave drains. Current runner is separate from historical continuous benchmark. This does not restore past F1 automatically or guarantee batch invariance across engines.

Validation:48unit tests passed. New tests prove slow-first barrier, preserved mode/cache dependencies and retries. Real fallback16 mixed pipeline finished145.382s, nofailures, all24keys;11waves verified to drain before next admission. No new fallback200 run or quality claim. fallback16/ has trace, lifecycle, requests, submission, report. Do not extrapolate this smoke runtime directly to200.
