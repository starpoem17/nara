이번 실험의 Macro F1은 0.165237이다. 실제 위반 153개 중 37개를 검출했고, 오탐은 167개, 미탐은 116개다.

판정 정확도 94.10%는 비위반이 많은 분포의 영향을 받는다. 모든 항목을 0으로 예측하면 정확도는 96.81%지만 Macro F1은 0이다. 주 지표는 Macro F1로 해석한다.

24개 중 11개 항목의 F1이 0이다: v4, v6, v7, v8, v12, v14, v15, v17, v18, v20, v23.
검색 가능했던 193건을 포함해 200건 모두 모델이 즉시 final을 선택했다. BGE-M3와 법령 인덱스는 로딩됐으나 검색 호출과 검색 결과 주입은 발생하지 않았다. 이번 수치로 RAG 효과를 주장할 수 없다.

원문은 절단하지 않았고 200건 모두 성공했다. 정답 라벨은 추론 입력에 들어가지 않았다. 프롬프트는 전체 추론 시작 전에 고정했고, 결과를 보고 추가 튜닝하거나 재추론하지 않았다.

재실행 시 기존 결과를 덮어쓰지 않도록 command.txt의 --output-dir을 새 경로로 변경해야 한다. 원래 실행 시점의 추론 코드와 프롬프트는 source/에 보존했다.
