# LJM original modules: independent dev200 run

User-approved scope: run each original module once, describe results in detail, commit; no success target, adoption decision, tuning or baseline comparison. Original files under `user/LJM/` are read-only.

Artifacts: `analysis/LJM_original_v2_v8_20260912/`. [Detailed report](../reports/analysis/LJM_original_v2_v8_20260912/report.md). [Previous experiment withdrawn](ljm-static-law.md).

- Scanner: unchanged `scan_candidates.py`, all 200 notices, v2–v8 candidate metrics; only input/output path globals redirected. No model or extractor output fed to this experiment.
- Extractor: unchanged `extract_candidates()` output passed in full to original `gemma_prompt(item, feature)` for each v4–v7 and every notice (800 calls). Candidate flags do not gate calls. Original extraction caps, duplicates and numeric parsing remain unchanged.
- Only approved prompt change: `관련 법령·고시금액을 검색한 뒤` → `제공된 법령·고시금액을 사용한 뒤`, followed by Korean verbatim excerpts from organizer-supplied laws. No old system prompt, criteria draft, extra full document, scanner hints, schema grammar or missing-source warning.
- Law spans selected/reviewed by Sol medium agents. `law_specs/` records exact source lines; `law_provenance.json` preserves raw and deletion-only cleaned text plus original source headers/hashes. Repeated titles, amendment history and XML debris removed; no translation, interpretation, rewritten rules or supplemented amounts. Effective dates are omitted from model input, retained only as original source metadata outside the prompt. Cross-referenced enabling, threshold and exception provisions included.
- Local Gemma4 26B A4B NVFP4 revision `5557756b8dce33ac72f2bd702b11729fdba3b839`; thinking OFF, temperature/seed 0, max context 32768, output 2048, batch8. Raw vLLM generation, no retries or judgment correction. These are integration settings absent from LJM's source, not claimed colleague-specified settings.
- `preflight.json` checks all 800 complete requests before inference. `manifest.json` freezes runner, scorer, original source, requests, extraction, legal text/specs and environment. Strict JSON/field diagnostics recorded separately. Invalid judgments are never filled with zero; valid-only scores and conservative all-record scores are distinguished if failures occur.
- Scores/cases describe this single observed run. No causal claim about hints, RAG or superiority, no independent-holdout claim for already examined dev data. Candidate metrics are not final model-judgment metrics. Not a complete 24-item competition submission.

Recalculate: `uv run --locked python analysis/LJM_original_v2_v8_20260912/evaluate.py`. For a fresh run, copy `run.py`, `score.py`, `evaluate.py`, `evaluation_protocol.json`, `law_specs/` into a new `analysis/LJM_.../` directory and execute prepare → run → evaluate; existing manifests/responses cannot be overwritten. Supplied dev files and local model must be present separately.

Evaluation-only format handling: observed responses use Markdown JSON fences. Frozen `score.py` remains unchanged; `evaluate.py` reads a complete single JSON fence without changing its payload and records the fence as a format violation. No JSON repair, arbitrary surrounding prose extraction or judgment replacement. This policy/hash is separately frozen in `evaluation_protocol.json`.

Observed run: 800 requests completed in 838.05s plus 59.46s model load; no length stops/retries. 787 readable judgments, 13 invalid (5 multi-block, 8 with outside commentary). Valid judgment totals TP7/FP62/FN18/TN700; invalid includes one positive gold. Per-feature F1 on valid judgments v4 .196078, v5 .111111, v6 .166667, v7 0; macro .118464, all-record conservative macro .107143. All responses violate bare-JSON formatting; those with a single complete code fence are read without changing fields. Scanner candidate TP23/FP10/FN24/TN1343, kept independent.
