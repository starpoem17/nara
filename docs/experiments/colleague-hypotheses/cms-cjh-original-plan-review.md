# CMS/CJH original-card fidelity review

Current scope: historical withdrawn design/observations. CJH redesign is cancelled; original CMS evidence was explicitly deleted. No legacy command below is an active execution instruction. [Current study status](cms-cjh-redesign-interview.md) and the linked canonical run reports govern reuse.

Date: 2026-09-13. Independent Astra medium review, before measured inference.

**Verdict: ready with conditions.** The agreed plan preserves the selected authors' criteria and explicitly identifies the approved integration changes. No direct source tampering is apparent in the plan. This is plan/source approval, not certification of an implementation or prepared requests. Complete the preflight evidence below before inference; no additional generic user approval is required for these checks.

Scope: read `AGENT.md`, `docs/README.md`, the agreed plan, the complete CMS `판정카드_v19-v24_정리본.md`, and CJH `v10.md`–`v18.md`. Did not read labels, previous errors/predictions, local prompts/rules, or reference-selection implementation. No inference performed. Line references below refer to these original source files, not a rewritten interpretation.

## Source comparison

CMS source: `user/CMS/판정카드v19_v24/판정카드_v19-v24_정리본.md`.
CJH source directory: `user/CJH/판정카드(10~18)/`.

| Source lines | Fidelity finding |
|---|---|
| CMS 3–51 (v19), 168–219 (v22) | Preserve authored timing/qualification distinctions and examples; no deterministic replacement. Plan lines 7, 24 do so. |
| CMS 55–106 (v20) | Full input-completeness fields are necessary (84, 92, 106); VAT, maintenance-period and missing-RFP cautions remain model instructions. Plan line 28 includes the relevant fields. Do not convert missing attachments into a forced 0 or 1. |
| CMS 110–164 (v21), 223–277 (v23) | Contract mode, adjustments/exceptions, and authored date-counting instructions must remain intact. Do not replace v21 with a numeric threshold check or v23 with local date rules. Plan lines 24, 31 preserve this. |
| CMS 281–339 (v24) | Preserve original metadata independently of notice text. The source's notice-priority instruction at 333 belongs to its supplied card; do not silently propagate it to other singleton cards. Plan lines 10, 28–29 permit exact singleton delivery. |
| CJH v10:28–42,55; v11:28–42; v12:27–39,54; v13:28–42; v14:30–44; v15:30–44; v16:30–44; v17:29–43; v18:30–44 | All nine cards require actual-purchase comparison with product names and special conditions. Preattached candidate rows implement only part of this information access. Plan lines 12–13 properly identify that choice as ours. Preserve all original metadata, especially the alternative/complementary registry evidence explicitly allowed in v10/v11:42. |
| CJH v13:12–20,42; v14:13–22,44; v15/v16/v17/v18:16–22 | Exceptions and date/contract/object-specific thresholds are material source conditions. References must retain applicable source conditions, dates and exceptions; an unexplained single current threshold is insufficient evidence of coverage. |
| CJH v15:44,57; v16:44,57; v17:43,56; v18:44,57 | Preserve cross-item exclusivity wording literally. Singleton execution is approved. Observe inconsistent outputs; do not infer a new coupled scheduler or reconcile their judgments. |
| CMS 102–106; CJH v10/v11:51–52, v16/v18:53–54; other cards' evidence sections | Authored absence-field and quotation rules remain in each card. The approved JSON wrapper is an integration interface; do not rewrite evidence instructions to match existing output conventions. |

## Conditions to close before GPU inference

1. **Prove exact delivery.** For all 3,000 requests, record unique notice/item keys, original source hashes and exact card spans. CMS sections must include all their examples/cautions, including v24:335–339; CJH files must be complete. Demonstrate that input fields/documents retain original values/text and that no local system prompt, rule, precomputed answer, or cross-item response appears. Save the actual template-rendered/tokenized requests, not only an intended prompt description. This implements plan lines 7–11, 24, 28–33.
2. **Freeze selection and coverage.** Plan lines 31–32 require artifacts not yet inspected in this review. Record exact selector fields, matching/scoring method, candidate cap/ties, and empty-result behavior before labels/inference. Publish actual complete rows/special notes, source hashes and match bases; supply no eligibility flags. Include references per item with original paths/spans/dates and an explicit coverage/gap inventory tied to the cards above. This does not require proving the selector has perfect recall. Selection omissions, irrelevant candidates, and missing historical references limit the experiment's interpretation; they must not be hidden or repaired after results.
3. **Prove the context budget.** Check every final chat-template request plus the reserved 512 output tokens against 32,768 using the actual model tokenizer. Plan line 33 already identifies tight headroom. Report maximum and overflow count before inference. An overflow blocks that run configuration until resolved; do not silently truncate notice/card/reference text. Surface any change to agreed supplied content or conditions to KHJ.
4. **Freeze parsing and denominators.** Before inference, decide whether a complete JSON code fence counts only as readable output; retain strict-format failure separately (plan 38). Declare how JSON `null` and `""` are assessed as blank evidence without changing stored output. Keep malformed/missing/extra/invalid-type outputs visible and excluded from ordinary 0/1 confusion counts with explicit denominators; never turn them into 0. These are evaluation conventions, not added model instructions or an interpretation of ambiguous card criteria.
5. **Verify runtime isolation.** Record one first-pass generation per notice/item, effective thinking-OFF settings, 512 output cap, maximum 16 active requests, completion-driven refill and prefix-cache statistics. Warmup must not inspect measured responses or tune decisions. Freeze predictions and input/reference hashes before the evaluation process reads labels (plan 14–15, 37–39).

## Approved limitations, not source-fidelity defects

- **Known source contamination:** CMS v24:335 explicitly identifies a dev violation, 337 supplies another dev-specific judgment, and 339 explicitly preserves `v24=1` for a named dev case while describing unresolved evidence. The user knowingly retained this text (plan 8, 40). Keep it intact and disclose that v24/full-dev results are not independent validation. Do not import `ITEM_RULES` merely because the original note names it. No newly read label is authorized during preparation.
- **Our retrieval/delivery experiment:** Product preselection and fixed preattached law excerpts are approved integration choices, not an authored retrieval algorithm or a guarantee of complete reference coverage. Even exact card text cannot isolate author quality from these choices. Report this distinction and the actual selection procedure, rather than attributing retrieval errors solely to CJH.
- **Our execution/interface conditions:** Singleton conversations, output512, thinking OFF and the neutral JSON wrapper were expressly approved. Preserve the cards' ambiguity under these conditions. Do not require joint judgments, longer reasoning, extra instructions, or post-generation repairs as fidelity fixes.

No mandatory change to the approved card content was identified. The concrete inference gate is completion of the recorded preflight checks, especially exact delivery, frozen reference/selector provenance and zero unresolved context overflows. This review does not claim those implementation checks have already passed.

## Follow-up: concrete selector and evaluation specification

Reviewed the preparation agent's specification before inference/labels; implementation and final artifacts remain unverified. **No additional hypothesis tampering identified; verdict remains ready with conditions.** The following concretizes our approved integration mechanism, not CJH's authored criteria:

- Select from the original 616 CSV rows by the union of exact 10-digit product-code matches in all notice documents or `meta.세부품명번호목록`, literal detailed-product-name matches of at least two characters in documents, and the five highest semantic scores. One-character names such as `표` and `요` are excluded only from literal-name matching; they remain eligible through other selection paths. This mechanical exclusion must be reported.
- Semantic queries cover the complete document text in BGE-tokenizer windows of 2,048 tokens with 128-token overlap, plus all metadata as a separate query. Each row's score is its maximum cosine similarity over queries. Use only CSV-row vectors from the existing organizer-only BGE index. Attach the union in CSV order with every original column/special note. Keep match locations, scores and selection reasons in audit artifacts only. No classification flags or skipped CJH calls.
- This design can select rows based on certificate mentions unrelated to the actual purchase, omit paraphrases or single-character names, and rank irrelevant rows highly. These are disclosed retrieval limitations; the unchanged cards still assign purchase identification and condition evaluation to the model (CJH v10:29–42,55; v12:28–39,54). No new prompt rule should resolve them.

Preflight should freeze the model/tokenizer/index hashes, exact indexed row text and row-to-CSV mapping, deterministic tie handling, and chunk construction. Establish that the reused vectors derive only from original organizer row content and neutral provenance, without local criteria/prompts or previous judgments. Verify that each encoded query fits the encoder limit without silent truncation, including the separate metadata query; document any chunk-boundary handling. These are provenance/reproducibility checks, not a demand for a different selector or additional user permission.

The proposed evaluation conventions are consistent with the plan: parse one complete JSON code fence as its unchanged payload while recording strict-format failure separately; reject arbitrary surrounding commentary and repair; preserve `null` and `""` distinctly but count both as blank solely for absence-field diagnostics. Compute confusion counts from valid judgments, and report all 200 cases through explicit invalid counts by gold label and attainable F1 bounds without imputing normal=0. Freeze exact validity definitions, bound calculations and zero-denominator conventions before reading labels. These conventions do not change model hypotheses or stored predictions.

## Follow-up: temporary implementation read-only review

Reviewed `tmp/colleague_cards.py` (470 lines at review time), plus only the encoder/index and engine execution plumbing it calls in `src/retrieval/search.py` and `src/inference/engine.py`. Did not read existing decision prompts, rules, labels, or previous outcomes. No code changes or inference. **No direct hypothesis tampering or label-dependent selector found. Readiness remains conditional on final preflight and the evidence gaps below.**

- Cards and inputs: lines 73–90 preserve complete CJH files and CMS preamble plus complete per-card sections, including v24 notes. Inputs use the approved allowlist and verbatim document text. Lines 227–240 build one user message from the neutral instruction, notice, original selected CSV rows, law excerpts and original card; no inherited system prompt or cross-item answer is added.
- Selector: lines 136–197 rank only mapped CSV-row vectors; each mapped passage is checked against original nonempty column values, source hashes and the expected 616 row numbers. Exact names, exact codes and stable top-five maximum cosine are unioned in CSV order. No amount/eligibility/violation decision or label read occurs. `BGEEncoder.encode` explicitly rejects encoder overflows before embedding, including metadata. `LegalRetriever` validates vector/passages hashes, dimensions and normalization. Queries and selected complete raw CSV lines are retained. Full model-weight identity still relies on declared revision/local provenance rather than a new weight-content hash.
- Generation: lines 375–386 override both schema-based sampling and reply parsing. The stream dispatches those overrides, so the base engine's registered structured-output backend does not itself supply a per-request grammar. Sampling specifies temperature=0, seed=0, 512 tokens and raw final-only output; thinking is disabled through chat-template rendering. Original output token IDs, special-token decoding and finish/stop reasons are retained for normal completions. No judgment recovery/retry path is called.
- Scheduling: lines 309–366 cap outstanding requests at 16, refill after completions, admit notices in source order and submit each feature once. The first feature delays the remaining features only to establish cache reuse; its answer is not consulted. Bounded live-notice/source-token admission may keep concurrency below 16, which is compatible with a maximum of 16. Runtime lifecycle and cached-token measurements remain necessary to establish observed execution.

Concrete gaps to close before the measured run:

1. **Rendered request evidence:** lines 241–245 retain message content and token count but not actual chat-template token IDs or their hash. Persist rendered-token evidence and verify the engine renderer matches preflight. Otherwise exact effective requests cannot be distinguished from messages rendered with another tokenizer/template.
2. **Exact-code provenance:** lines 166–184 retain an `exact_code` reason but no matched document/meta location. Add the promised code-match locations, analogous to the stored name-match offsets. This is an audit-data fix; it must not change candidate selection.
3. **Failure preservation:** lines 439–446 persist timing/lifecycle in `finally`, but persist admission/scheduler events only after success. Raw completions held in `model.raw_outputs` and pending task statuses can also be lost if `stream.poll()` fails its metrics check after decoding. Preserve available raw output, exception and pending/aborted task evidence on failure, without retries or substituted judgments. Normal successful-response logging is already immediate.

The reported long-notice/v20 reference overflow is a concrete unresolved configuration constraint, not permission to shorten source material. Complete and retain all-request preflight, then surface the exact affected requests and content/condition options to KHJ as planned. This static review does not certify zero overflows or a completed execution.

## Root preflight closure after user context approval

The following verification was performed by the implementing root agent, not independently certified by Astra. All 3,000 final requests passed `experiments/legacy_CJH_original_cards_v10_v18_20260913/code/verify_colleague_cards.py`: exact original card and notice fields, complete raw selected rows, law spans/header provenance, frozen input/artifact hashes and actual chat-template token IDs. Prompt hashes equal the preserved 32,768-context draft. Maximum input+512: CMS 34,983 / CJH 35,897; zero overflow at the user-approved experiment-only 36,864 ceiling. Maximum BGE query length was 2,052, below the encoder limit. The audit guard blocked label/local-criteria/KHJ/LJM access during preparation and verification. Results: each suite’s `preflight_verification.json`.

The three implementation gaps in the preceding Astra review were addressed before freezing: actual rendered token IDs/hash with runtime equality checks; exact-code match locations without altering candidates; and exception/pending/raw-output/scheduler preservation. The 22 focused engine, scheduling and evaluation tests passed before inference using synthetic judgments. Runtime isolation and metrics remain subject to the completed-run audit.

## Root completed-run audit

The implementing root agent verified both completed suites and froze raw-output hashes at 2026-09-12T17:35:33.345799Z before opening labels. All 3,000 unique notice/feature requests were generated once, exact frozen input-token counts matched runtime, every output stayed within 512, and seed completion preceded independent follower submission. Observed maximum inflight was CMS 15 / CJH 16; scheduler preemption statistics were zero. Evidence is each suite’s `runtime_verification.json`. This closes the requested runtime evidence condition but is not an additional independent Astra review. Output-schema mismatch and all other outcomes are reported without repair in the [results](<cms-cjh-original-results.md>).
