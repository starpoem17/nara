# CMS updated-card fidelity review — Astra medium original output

Reviewed 2026-09-13 by `/root/cms_updated_astra_review`, model `gpt-6-astra`, reasoning `medium`. Independent read-only source/plan review. Root preserved the returned text below verbatim. [Reviewed plan](../experiments/cms-updated-cards-plan.md). Implementation approval was pending at review time. KHJ later settled the presentation choices and explicitly approved execution; see the [CMS decision record](../experiments/cms-updated-cards-decisions.md).

---

**판정: 조건부 충실.** 현재 계획은 CMS의 갱신 판정카드와 업로드 후보를 이용한 **dev200 추론 실험**으로는 대체로 충실하다. 다만 후보의 문서·조각 경계를 모델에 어떻게 전달할지는 판단과 인용에 영향을 줄 수 있어, 구현 승인 전에 사용자 결정이 필요하다. 추출을 포함한 전체 파이프라인의 성능이나 미관측 데이터 일반화를 검증하는 계획은 아니다.

검토 근거는 커밋 `615e649e80794556f9ae2d889da99d5c41a8cba4`의 아래 두 Markdown 전문과 `candidates.jsonl`이다. JSONL의 지정 SHA256, 200행 및 행별 여섯 항목을 직접 확인했다. 구현·추론·테스트는 수행하지 않았다.

- **카드**: `user/CMS/판정카드v19_v24/판정카드_v19-v24_토큰수_제한.md`
- **추출규칙**: `user/CMS/판정카드v19_v24/원문_META_추출규칙_v19-v24.md`

**1. 확정 계획에서 충실한 부분 — 사용자 재결정 불필요**

여섯 독립 프롬프트, 항목별 원래 판단기준·예외·확인 불가 처리, 채팅 템플릿을 포함한 입력 2,000토큰 상한, 원래 항목 키를 사용하는 JSON 출력은 카드의 명시 요구와 맞는다. `input_completeness`와 `dropped_doc_counts`를 모든 항목에 전달하는 것도 추출규칙 11–13행에 부합한다.

META를 원래 필드명·값의 일반 텍스트로 넣고 후보 원문으로 자리표시자를 치환하는 방식은 이미 승인됐다. 별도 판단 프롬프트나 법령 묶음을 추가하지 않는 선택도 충실하다. 카드 v24가 `data/항목표.json`을 언급한다고 해서 그 파일 내용을 추가로 주입할 필요는 없다.

**2. 명시적으로 승인된 변경과 결과의 범위 — 사용자 재결정 불필요**

사용자는 추출을 다시 수행하지 않고 CMS의 JSONL을 그대로 사용하며, 카드 169행의 정답 메모를 제외하도록 승인했다. 따라서 결과는 **정답 메모를 제외한 갱신 카드가 CMS의 기존 후보에서 내리는 판단 성능**을 보여준다. 추출기의 재현성·누락률·실행시간까지 검증한 결과로 표현하면 안 된다.

추출규칙 3행은 dev 및 정답을 개발 기준으로 명시하고, 58–71행은 dev에서 후보 회수를 점검했다고 밝힌다. 메모 한 줄을 제거해도 이 개발 이력은 사라지지 않는다. 기존 후보와 카드의 dev 성능으로 보고하고, 미사용 검증셋이나 전 과정 정답 비노출 실험으로 부르지 않는다는 계획은 적절하다.

**3. 문서·조각 경계 생략 — 구현 승인 전 사용자 결정 필요**

계획은 후보의 `text`만 두 줄바꿈으로 연결하고 문서 ID·유형·위치를 모델에 보여주지 않는다. 추출규칙 8행은 이 정보를 **보존**하라고 했으므로 감사 산출물에 보존하는 것이 명백한 원문 위반이라고 단정할 수는 없다. 모델 노출을 명시적으로 요구한 문장도 없다.

그러나 추출규칙 13행은 조각 사이에 새 근거를 만들지 말라고 하고, 카드 145행은 날짜가 서로 다른 문서에 있으면 이어 붙이지 말고 한 구간을 인용하도록 한다. 대부분 항목도 한 문서의 연속 원문을 요구한다. 원문 내부에도 빈 줄이 있으므로 두 줄바꿈만으로는 모델이 문서 전환과 생략 구간을 확실하게 식별할 수 없다.

이는 가상의 문제가 아니다. 실제 JSONL의 다문서 후보는 v19부터 v24 순서로 **28·12·35·11·61·70건**이며, 동일 문서의 비연속 구간도 존재한다. 경계 정보 생략은 문맥 연결과 인용 선택에 영향을 줄 수 있다. 사후 원문 검사는 잘못된 인용을 발견하지만, 추론 당시 없었던 정보를 복구하지는 못한다.

따라서 사용자가 **현재 표현의 경계 정보 손실을 수용한 범위로 실험을 승인할지, 모델에 전달할 경계 표현을 추가로 정할지** 결정해야 한다. 임의로 헤더를 추가해서도 안 된다. 표현이 달라지면 현재의 최대 1,998토큰 측정은 그대로 적용되지 않으며, 후보를 임의로 잘라 맞추는 것도 승인 범위를 벗어난다.

**4. 후보 자체의 관측 한계 — 보완 대상이 아닌 보고상 제한**

JSONL에는 `완전관측=true`와 `budget_truncated=true`가 동시에 있는 v20 입력이 3건 있다. 문서 처리의 완전성과 모델에 전달되는 후보의 완전성은 같지 않다. 따라서 완전관측 값만으로 모델이 모든 관련 문구를 보았다고 주장할 수 없다.

이는 업로드 후보와 1,800자 제한에 이미 존재하는 특성이다. 추출규칙 71행의 후속 개선 제안도 현재 후보에 구현됐다는 뜻은 아니다. 사용자가 승인한 후보를 보완하거나, 절단 여부로 판단을 강제하거나, 새 부재탐지 규칙을 추가할 근거로 삼으면 안 된다. 결과 해석에서 한계를 밝히면 된다.

**5. 승인 후 일반 검증 — 새 가설을 추가하지 않고 확인할 사항**

- 최종 입력의 원문·META·순서, 필수 관측 필드, 승인된 메모 한 줄 제거 및 1,200개 실제 채팅 입력의 토큰 수를 확인한다. 현재 길이 측정은 준비 가능성의 근거이며 실행 검증은 아니다.
- 구조화 디코딩은 합의한 항목 키·필드·자료형과 0/1 선택을 보장하는 수준으로 제한한다. 정답, 예외 적용, 근거 내용이나 판단과 근거의 의미 관계를 코드로 강제해서는 안 된다. 실패 출력도 정상 0으로 바꾸지 않는다.
- 추출 시간은 미측정으로 명시하고 입력 준비·모델 로드·추론 시간을 분리한다. 측정 범위에서 빠진 추출 작업 때문에 전체 원문→추출→추론 시간보다 짧은 범위라는 설명은 타당하지만, 누락 시간이나 전체 소요시간을 추정해 만들어서는 안 된다.
- 동일 접두어 캐싱은 작성 순서를 유지한 실제 공유 접두어에만 해당한다. 항목 간 전문 공유나 캐시로 인한 가속 효과를 자동으로 입증하지 않는다. 실제 설정·관측량과 측정 구간의 캐시 상태를 기록해야 한다.
- 예측을 고정한 뒤 정답 평가와 원문 연속성·길이 검사를 수행한다. 이는 이후 실행의 정답 유입을 막지만, CMS 자료의 dev 기반 개발 이력을 없애지는 않는다.

현재 단계에서는 위 경계 표현 문제와 계획의 한정된 검증 범위를 사용자에게 보고한 뒤, 명시적인 구현 승인을 기다리는 것이 맞다.
