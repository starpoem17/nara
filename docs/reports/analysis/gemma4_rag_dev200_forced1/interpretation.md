Artifacts: [run directory](<../../../../analysis/gemma4_rag_dev200_forced1>). Unlinked artifact names below are relative to that directory.

# 검색 1회 강제 결과 해석

Macro F1: 0.165237 → 0.164157 (차이 -0.001080).
위반 검출은 37/153에서 34/153으로 감소했다. 오탐은 167→123, 미탐은 116→119, 완전 일치 공고는 49→66이다.
주 지표가 조금 낮아 사용자가 지정한 후속 thinking 실험의 검색 설정은 비강제를 선택했다.

200건 모두 검색 1회와 비어 있지 않은 검색 자료를 확인했다. 총 질의는 676개이며 검색 실패와 빈 검색 결과는 0이다.
실행 시간은 325.75초에서 563.97초로 늘었다. 측정된 검색 시간은 0.670초이며, 모델 호출 시간은 295.63→515.34초다. 따라서 추가 비용의 대부분은 추가 모델 호출과 늘어난 입력 처리에서 발생한 것으로 해석한다.

가장 큰 항목별 변화는 v22의 F1 0.750→0.333, v23의 0.000→0.333이다. 전체 평균 차이는 작으며 dev 200건의 탐색적 비교다.

변경 범위: 최초 응답을 검색 JSON으로 강제, 검색 한도를 정확히 1회로 설정, 검색 안내 한 문장 변경. 긴 입력에서 검색어 생성 한도만 남은 문맥에 맞춰 줄였다. 최종 출력 한도 2048, 원문·검색 passage, 모델·인덱스·판단 지침은 유지했다.
